import { AlcoholStatusDirective } from './alcohol-status.directive';

describe('AlcoholStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new AlcoholStatusDirective();
    expect(directive).toBeTruthy();
  });
});
